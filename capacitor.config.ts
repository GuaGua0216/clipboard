import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guagua.clipboard.mobile',
  appName: 'GuaguaClipboard',
  webDir: 'dist'
};

export default config;
